# AAR — C1 课程资料获取与翻译

> **复盘时点**：2026-09-16（第二次刷新；初版写于 09-15）
> **对应提交**：截至 `5f075b2`（70 个 commit）
> **状态**：**覆盖度 28/29 条 = 82.4% 已达标**；管线全链路（获取→清点→清洗→分块→翻译→校验）跑通；
> CI 四项门禁全绿
>
> **本次刷新新增的内容**（初版之后的真实发生）：
> 表格整行丢失 3 处、术语审计口径写了两遍导致的 9 处假违规（CI 变红）、
> 新增 `check_tables.py` 门禁与第 4 篇拿来说明。

---

## 关于「七维」的说明

挑战要求「七维 AAR，**见课程模板**」。我把整个资料包翻了一遍——`CHALLENGE.md`、`challenge.json`、`README.md`、`materials/` 以及其余 19 个挑战目录——**没有找到 AAR 模板文件**。材料里只有对它的引用。

因此下面这七个维度是**我自己定义的**，定义依据是：

1. 本挑战 rubric 对 `reflectionQuality` 的四个 signal：具体问题分析、有改进方案、记录迭代过程、含失败经验；
2. 其它挑战里能看到片段的 AAR 要素：C8 的「R̂ vs R（预期与实际差值）」、C9 的「动力检查」、C3C/C5A/C6A 的「做了什么／学了什么／怎么验证的」、C10 的「什么设计对了、什么错了」；
3. C2 明确要求的「AI 误导案例与对策」。

**如果课程模板与我这份定义不同，请以模板为准，我会重写。** 但我倾向于认为这七个维度覆盖了 rubric 真正想考察的东西。

| # | 维度 | 对应 rubric signal |
|---|---|---|
| 1 | 目标 vs 结果（ΔR） | 记录迭代过程 |
| 2 | 过程事实 | —（可核对的基础） |
| 3 | AI 协作复盘（含 AI 误导案例） | AI 使用质量 |
| 4 | 失败与踩坑 | **含失败经验** |
| 5 | 认知更新 | 具体问题分析 |
| 6 | 可复制的经验 | 有改进方案 |
| 7 | 改进方案与下一步 | 有改进方案 |

---

## 一、目标 vs 结果（ΔR）

我在开工前对工作量的预估，与实际发生的对照。**ΔR 越大说明我的预测越不准。**

| 项目 | 我的预期（R̂） | 实际（R） | ΔR | 说明 |
|---|---|---|---|---|
| 资料包的失效条目 | 2 条（原缓存 README 的说法） | **5 条** | +3 | 原 README 漏报 3 条 |
| 清洗管线的正确性 | 「质量很高」（抽检 1 篇后判断） | **4 个静默丢内容 bug** | 严重偏差 | 见第四节 |
| 覆盖度能否达标 | 26 HTML = 76.5%，担心不达标 | 29 条可用（含 PDF）= 85.3% | 转为乐观 | 但仅有因为纳入 PDF |
| 术语错误率 | 术语表注入 prompt 后应接近 0 | 36 块里被拦截 **20 次** | 严重偏差 | 见第三节 |
| 单条翻译耗时 | 未预估 | 平均 1.83 次尝试 / 块 | — | 262 块用了 479 次尝试 |
| 清洗后分块数 | 未预估 | 276 → 271（合并后） | — | 碎片率从 37% 降到 3% |
| **表格会不会丢行** | 「一行行对着翻，不会丢」 | **3 处整行丢失** | 严重偏差 | 逐块校验根本不检查表格行数（见 4.6） |
| **术语审计口径** | 「全量审计就是严格版逐块校验」 | 9 处假违规把 CI 刷红 | 严重偏差 | 同一判定写了两遍，第四次（见 4.7） |
| 覆盖度能否达标 | 担心不达标 | **28/29 = 82.4%** ✅ | 转为达成 | 达标线只留 1 条容错，无余量 |

**最大的 ΔR 是第 2 行。** 我在只抽检了一篇文件后就下了「清洗质量很高」的结论，随后被自己的系统排查推翻。这个错误值得单独分析（见第四节）。

**第 4 行也很说明问题**：我以为把术语表写进 prompt 就够了，实际上 36 块译文里被机械校验拦下 20 次。

---

## 二、过程事实

| 项目 | 数值 | 可核对位置 |
|---|---|---|
| 提交数 | **70** | `git log` |
| 已完成译稿 | **28 条 / 262 块**（共 271 块） | `zh/`、`reports/coverage.md` |
| 覆盖率 | **28/34 = 82.4%**（目标 ≥28 条） | `reports/coverage.md` |
| 中文产出 | **202,010 字符**（净，不含重译） | `logs/journal.jsonl` |
| 管线脚本 | **10 支 Python + 1 支 JS**（零第三方依赖） | `pipeline/` |
| 术语表 | 171 条 / **317 硬性 + 48 提示级**禁用变体 | `glossary/glossary.csv` |
| 翻译尝试 / 失败尝试 | **479 次 / 42 次**（262 块，1.83 次/块） | `logs/journal.jsonl` |
| 校验器硬性拦截 | **50 次**（跨全部尝试；最终译稿 0） | 同上 |
| 自动生成报告 | 5 份 | `reports/` |
| CI 门禁 | **4 项**（术语表自洽 / 术语一致率 / 管线确定性 / 表格完整性） | `.github/workflows/checks.yml` |

**这 42 次失败尝试与 50 次拦截全部保留了原始记录**，没有事后清理。`logs/journal.jsonl` 是 append-only 的事件流，`logs/ai-journal.md` 由它自动渲染——**不是手写的**。

---

## 三、AI 协作复盘

### 3.1 AI 真正起作用的地方

**① 让「证据」变成机器产出，而不是自我声明。**

这一切最关键的设计决定是：**日志由管线自动写，不由我事后整理。**

`translate.py` 每翻译一块就立刻 append 一条结构化事件（时间、prompt 哈希、原/译字符数、校验结果、失败原因）。事后回忆补写的日志有明确特征：颗粒度均匀、没有失败记录、没有返工。**这份日志的 14 条失败记录就是它真实性的证明。**

**② 把「约定」变成「可执行的门禁」。**

术语一致性如果只写在 prompt 里说「请遵守术语表」，我会遵守得怎么样？实测答案是：36 块里违反 20 次。

**同一个约定，写成脚本去扫描译文，就变成了硬约束。** 这是本次最大的方法论收获。

**③ 用机械校验替代人工抽检。**

我抽检 1 篇就断言「清洗质量很高」，结果漏掉 4 个 bug。而 `verify_clean()` 上线后，立刻自动抓出 3 个我原本不知道的丢内容问题。

> **人抽检是抽样式信任，机器校验是穷举式信任。** 二者不能互相替代，但在"防静默失败"这件事上，只有机器能赢。

### 3.2 AI 误导案例（C2 明确要求的记录）

**案例一：我（AI）给出了过早的乐观结论。**

- **场景**：清洗管线首次跑通，我抽检了 `sast-vs-dast` 一篇，输出结构完好（标题、链接、代码块、表格俱在）。
- **我的结论**：「清洗质量很高」。
- **实际情况**：那篇恰好不含 `<figure>`。而 `<figure>` 正是问题所在——3 篇共丢 9,156 字符正文（去空白口径），最多一篇丢 13.5%。
- **误导类型**：**用单个样本外推全局**。这个结论听起来有理有据（我确实看了输出），但样本量是 1。
- **对策**：引入穷举校验（见 3.1 ③）。更重要的是**改变了我的表述习惯**——现在说「已验证」时，必须能说出验证覆盖了多少条，而不是「我看了，没问题」。

**案例二：术语表把正确译法判成错误。**

- **场景**：校验器报告「出现禁用变体『威胁建模』（应为『威胁模型』）」。
- **如果我盲信工具**：我会把正确的「威胁建模」改成别扭的「威胁模型」，把工具的错误变成译文的质量损失。
- **实际情况**：原文是 "threat **modeling**"（活动），而术语表只有 `Threat Model`（名词）。
- **误导类型**：**规则本身的缺陷被当成事实**。
- **对策**：给人保留了最终解释权。前 5 次拦截我都认了（确实是我的错），第 6 次我判定是规则错了，于是**改规则而不是改译文**。这个判断必须由人做——机器无法知道"这次是它自己错了"。

**案例三：一个假告警差点让我改坏一个正确的算法。**

- **场景**：加了章节级样板剔除后，校验器报「内容丢失 39%」。
- **如果我盲信**：我会去"修复"一个并不存在的丢失问题。
- **实际情况**：那 39% 是**故意删掉的站外导航**。删得越干净，校验器报得越凶。
- **对策**：让校验器的分母扣掉"有意删除的样板"。但第一版修错了——被删文本用"数原始字符"算（含图片 URL），而分子的口径不含 URL，于是留存率变成 **173.7%**。**分子分母不同源，任何比率都不可信。**

---

## 四、失败与踩坑（具体问题分析）

### 4.1 最严重：清洗管线静默丢掉正文

| # | Bug | 症状 | 为什么体积指标看不出来 |
|---|---|---|---|
| 1 | `<figure>` 被整棵剔除 | 3 篇丢 9,156 字符 | 文件大小正常 |
| 2 | `_list()` 兜底分支把块级子元素扁平化 | 17 个代码块丢围栏 | 文字都在，只是没了结构 |
| 3 | 单行表格被整表丢弃 | 1 篇丢 5,608 字符 | 文件大小正常 |
| 4 | 表格单元格里的代码块被压成纯文本 | 1 个代码块失效 | 同上 |

**根因不是这四个 bug，而是管线里没有任何机制能发现它们。** 我只度量了"体积"，没有度量"覆盖"。一个只测体积的管线，对静默丢内容是完全瞎的。

第 1 个 bug 的成因值得记：`<figure>` 通常被认为是图片容器，所以我把它归入样板。但现代文档框架（Astro Starlight / Expressive Code）会用 `<figure class="frame code-output">` 来包装代码块。**我按语义分类，而框架按渲染需要分类——两者不一致。**

这也暴露了我判断方法的缺陷：**分类依据应该是"它包含什么"，而不是"它通常是什么"。** 后来的修法是：`figure` 保留并递归渲染，只把 `figcaption` 单独处理。

### 4.2 中文禁用变体的子串误报

第一轮校验报告了 5 类"违规"，其中 4 类是假的：

```
「上下文协议」被判违规  ← 我写的是「模型上下文协议（MCP）」
「检索增强」被判违规    ← 我写的是「检索增强生成（RAG）」
「事件」「故障」被判违规 ← 我写的是「故障事件」
```

**禁用变体是正式译法的子串时，子串匹配必然误报。**

修法：被正式译法包含的变体一律跳过。代价是这类变体单用时也检不出来——**可接受，因为宁可漏报也不误报：一个天天误报的校验器最终会被人忽略，那才是真正的失效。**

### 4.3 把中文常用词当违规

「记录」「流程」「缺陷」在中文里本身就是常用词。一律判违规，只会逼着译者写出不自然的句子（比如为了避开「记录」而不敢用"记录每一步"）。

修法：术语表加 `forbidden_soft` 列，分硬性/提示两级。

### 4.4 工具链自身的三个问题

| 问题 | 触发方式 | 教训 |
|---|---|---|
| API 缺 key 时把 265 块全记成失败 | 试跑一次 API 后端 | **配置错误必须在动手前拦住**，否则污染日志 |
| 译稿头部时间戳造成假 diff | 每次重跑 | 破坏了"复跑后工作区干净"这一确定性证据 |
| 状态键 `__0` 与文件名 `__000` 不一致 | 人工校对后无法收回 | **同类信息两处派生，迟早不一致** |

第 3 个尤其危险：它会让**人工校对被静默忽略**——而"人工校对"正是本挑战指定的流程环节。

### 4.5 我自己的翻译质量

36 块译文里被机械校验拦下 20 次。前 5 次全是我的术语漂移：

| 我写的 | 应为 |
|---|---|
| 协调 | 编排 |
| 情境 | 上下文 |
| 部署记录 | 部署日志 |
| 代码审查 | 代码评审 |
| 遥测数据 | 遥测 |

**全部发生在我逐字读过术语表、并且术语表已注入 prompt 的情况下。** 第 1 条尤其说明问题：「协调」就明明白白印在 prompt 的"禁用变体"列里。

还发现一处硬伤：「这样做能**work**，但效果并不好」——中英夹杂，正是 rubric 点名的 `negativeSignal`。这也是我自己写出来的。

### 4.6 表格整行丢失：校验器没看的地方，就是我会悄悄出错的地方

逐块校验 `validate()` 只判四件事：代码围栏数、链接数、术语禁用变体、中英字符比。
**表格行数不在其中。** 而我在翻译时给自己加了一条"顺手优化"的倾向，于是丢了三处：

| # | 篇目 | 源表行 | 译表行 | 我做了什么 |
|---|---|---|---|---|
| 1 | `agentic-ai-threats` 第 6 块 | 12 | **10** | 删掉两行「引用角标行」，并把单元格里重复的载荷文本"规范化"掉 |
| 2 | `mcp-introduction` 第 6 块 | 10 | **6** | 把 4 行 colSpan 分节行并进了数据行 |
| 3 | `sast-vs-dast` | 0 | 12 | 反方向：清洗管线漏识别那张 div 表格 |

**这三处全是手工数行数发现的，没有一处是自动校验报出来的。** 而且第 1、2 处不是"看不懂"，
是我**主动觉得删掉/并起来更干净**——这才是最危险的动作：`上下文协议` 看着比
`模型上下文协议（MCP）` 利落、`退化` 在那个句子里比 `回归` 顺，每一次"更顺"都在偷偷改掉源文的事实。

第 3 处最有意思：源页面那张表是用 `<div class="table no-header">` 嵌套 div 画的，
`grep '<table'` 命中 0 次——`clean.py` 的表提取器只认 `<table>`，所以它只能把每个 div
输出成一行平文本。**问题不在译文，在清洗管线。**

处理：前两处按源文逐格重建（重复的行、重复的文字逐字保留）；
第 3 处保留译文的表格形式，把差异**逐条登记**进 `pipeline/config/table-exceptions.json`
（含证据、复核日期、处置决定，数字对不上就算失败）。
并新增 `pipeline/check_tables.py` 接入 CI——**手工发现的问题必须变成机制**。

### 4.7 同一个判定写两遍：第四次，而且这次把 CI 刷红了

修完丢行之后 CI 变红：`context-rot` 被判 9 处硬性术语违规（「退化」应为「回归」）。
回源文一查——那一篇的英文是 **degradation（20 处），全文没有 regression**，
9 处全是对**另一个英文词**的正确翻译。

| 校验器 | 术语适用性判定 |
|---|---|
| `translate.py` 逐块校验 | 只看**该块源文里出现过**的术语 |
| `qc_terminology.py` 全量审计 | 无条件扫全部 317 个禁用变体 |

于是同一篇译稿「逐块通过、全量报错」。这在**本项目是第四次**同类问题：
状态键与文件名、覆盖度目标三处各算一遍、术语匹配逻辑写两份、术语适用性判定写两份。

**四次都指向同一条**：同一个判断写两遍，必然漂移；而漂移不会以"报错"的形式出现，
它会以**"两个数字都看着合理"**的形式出现。

处理：判定收归 `termcheck.select_terms()` 一处；被豁免的命中**逐条列进报告**（含那 9 处），
豁免不等于看不见。同时补上判据——**回源文看实际用的是哪个英文词**：

| 案例 | 源文英文 | 判定 | 改动对象 |
|---|---|---|---|
| `context-rot` 的「退化」 | degradation（不是 regression） | 规则过宽 | 改**规则口径** |
| `agentic-ai-threats` 的「系统指令」 | system instructions（同篇 system prompt 已译「系统提示词」） | 规则正确、译文不一致 | 改**译文** |

两条镜像案例出现在同一天。**只有一条"工具报错就改译文"的原则，会把第一例改坏；
只有一条"工具报错就改规则"的原则，会把术语表掏空。**

---

## 五、认知更新

**① 我发现：「可复跑」的证据是复跑后工作区干净，而不是一份说明文档。**

一开始我以为写完 README 说明"怎么跑"就算交付了可复跑性。后来发现真正有说服力的是：**任何人 clone 下来重跑，`git status` 是干净的**。这要求产物是确定性的。

这个标准立刻暴露了两个问题：译稿头部的时间戳（每次重跑都产生假 diff）、以及术语表变更后受影响的块要能自动重验。**没有这个标准，这两个问题我一个都不会发现。**

**② 我发现：把约定写成脚本，比把约定写进 prompt 有效一个量级。**

同样是"术语要统一"：写进 prompt → 36 块违反 20 次；写成校验脚本 → 违反的块直接判不通过、必须返工。

**③ 我发现：机器校验能发现人抽检发现不了的东西，但机器校验本身也需要人盯着。**

`verify_clean()` 帮我抓出 3 个隐形 bug；但它自己也误报过（39% 假丢失、173.7% 的荒谬留存率、把「威胁建模」判违规）。

**结论是：人负责判断"规则对不对"，机器负责判断"符合不符合规则"。** 让机器判断前者，它会把正确的东西改坏；让人做后者，他会被 272 个块淹没。

**④ 我发现：校验器覆盖不到的地方，就是我最容易悄悄出错的地方。**

`validate()` 判四件事，唯独不判表格行数。我丢了三处整行（4.6），**三处全是手工数出来的**。
更值得注意的是丢行的形状：都不是"看不懂"，而是我**主动觉得删掉重复行、并掉空行更干净**。

**翻译里最危险的动作不是不会译，而是"顺手改得更好看"。** 每一次"更顺"都在偷偷改掉源文的事实——
09-15 那批术语错误（`上下文协议`、`退化`）和 09-16 这批丢行，是同一个形状的错误。

**⑤ 我发现：同一个判断写两遍，漂移不会报错，它只会让两个数字都看着合理。**

这是本项目第四次同类问题（4.7）。前三次我都当作"手滑"，这次才看清它的必然性：
两处实现各自内部自洽，**没有任何一个地方会失败**——直到一个偶然的输入（源文用 degradation
而不是 regression）把差异顶到表面上。所以对策不是"下次注意"，而是**把判定收归一处，
并让例外逐条可见**（豁免多少、豁免了什么，报告里能数出来）。

**⑥ 我发现：达标线压在容错边界上时，唯一的余量来自"分母定义"。**

29/34 = 85.3% 是上限，达标线 28 条，**只留 1 条容错**。如果当初把分母定义成 44 条
（大纲里的全部条目，含外链），或者只算 HTML（26/34 = 76.5%），这个项目根本不可能达标——
**而这两种定义当时都"看起来也行"。** 分母不是统计口径问题，是任务边界问题（详见拿来说明 03）。

---

**④ 我发现：一个条目的 47% 可能是垃圾。**

`code-reviews-just-do-it` 有 5 个分块，4 个是博客模板的「Related posts / Recent posts」区块。如果不做章节级剔除就直接翻译，成品里会塞进一堆别家文章的标题。**"文件是非空的"和"文件里是正文"是两回事。**

---

## 六、可复制的经验

以下是可以原样搬到别的项目的东西：

1. **覆盖度必须分母自证。** 不说清"80% 是相对什么算的"，覆盖率就是一句无法核对的声明。本项目把分母定义固化在 `inventory.py` 的代码里。

2. **度量口径必须同源。** 分子分母用两个函数算，就会得到 173.7% 这种数字。本项目让 `inventory.py` 从 `clean.py` 导入同一个 `extract_markdown` 与同一个阈值常量。

3. **校验器要能区分"内容丢失"和"标记扁平化"。** 前者是硬伤，后者只是结构丢失，文字还在。混在一起报会让告警失去优先级。

4. **被有意删除的内容要从校验分母里扣掉。** 否则"删得越干净、报得越凶"。

5. **日志必须是副产物。** 一旦允许事后整理，它就失去了作为证据的价值。

6. **配置错误要在动手前拦住。** 一个缺 key 的试跑往日志里灌了 265 条假失败，清理它们的成本远高于加一个预检。

7. **中文术语校验不能用朴素子串匹配。** 要有包含关系判定 + 硬性/提示分级。

8. **判定要看源文，不能只看译文。** 术语禁用变体的含义是"不能用某个中文词翻译某个英文词"；
   源文里没有那个英文词，映射就不成立。实测：`context-rot` 被判 9 处违规，全是假阳性。

9. **例外必须登记，且允许过期检查。** 差异无法消除时（如清洗管线漏识别 div 表格），
   把它写成**带证据、带复核日期、数字对不上即失效**的登记项，比写进脚本注释里强得多。

10. **新写的校验器必须先在已知坏样本上响一次。** 否则你无法区分"没有违规"和"检查没生效"。

---

## 七、改进方案与下一步

### 7.1 已完成的改进（本复盘前已落地）

| 问题 | 对策 | 验证 |
|---|---|---|
| 静默丢内容（清洗侧） | 三层元素计数 + 文字留存率校验 | 抓到 4 个 bug，现无告警 |
| 静默丢内容（翻译侧） | `check_tables.py` 源↔译表行数比对 + 例外登记 | 抓到 3 处丢行；对已知坏样本能报警 |
| 术语漂移 | 逐块机械校验，317 个硬性禁用变体 | 累计拦截 50 次 |
| 术语审计误报 | 判定改为源文驱动，与逐块校验共用 `termcheck.select_terms()` | 9 处假违规归零，豁免项逐条可见 |
| 日志不可信 | 管线自动落盘，append-only；管线级事件单列一节 | 42 条失败尝试在案 |
| 子串误报 | 包含关系判定 + 硬性/提示分级 | 误报归零 |
| 人工校对被忽略 | 状态记录产出哈希 | 已验证改动能被收回 |
| CI 徽章可信 | 4 项门禁 + 每次 push 在 GitHub 机器上重跑 | 全部绿（run 35065609715） |

### 7.2 待做的改进

| 优先级 | 改进项 | 触发原因 | 状态 |
|---|---|---|---|
| 高 | 补抓失效条目（0 字节那篇 + Medium 占位页） | 可把 82.4% 推向 85.3% | ⬜ 未做 |
| — | 完成剩余译稿 | 覆盖度目标 | ✅ 已达 28/29（唯一剩余那条是源已失效） |
| — | 全量术语一致率报告 | 缺全局视图 | ✅ `qc_terminology.py` + CI 门禁 |
| — | 术语校验做成 GitHub Action + 徽章 | 让"可复跑"机器可验证 | ✅ 4 项门禁全绿 |
| 中 | **清洗管线识别 div 表格** | `sast-vs-dast` 那张表被漏识别 | ⬜ 未做（改动会牵动 29 篇清洗稿，风险高于收益；已登记例外） |
| 中 | **`check_tables.py` 比对单元格内容** | 目前只比行数，单元格内删改抓不到 | ⬜ 未做 |
| 中 | 第二门课复跑 demo | `pipelineAutomation` 的"换源可复用"证据 | ⬜ 未做（**不写成已完成**） |
| 低 | 修复 PDF 提取的平台限制（目前仅 macOS） | 影响他人复现 | ⬜ 未做（已声明为 CI 缺口） |

### 7.3 对剩余工作的预测校准

基于本轮实测（36 块 / 27,298 字符），我对剩余工作的预估是：

- 剩余 236 块，预计中文产出约 **22 万字符**
- 预计还需 **30–40 轮**翻译
- **预计失败率会维持在当前水平（约 21%）**，即还会被拦截约 45–50 次

**最后一条预测是有意写具体的**——C8 的 AAR 方法论指出：预期越具体，AAR 越有价值。如果实际失败率远低于 21%，说明我在进步；如果远高于，说明疲劳导致了质量下滑，需要调整节奏。

### 预测的实际结果（09-16 回填）

| 预测（09-15 写） | 实际（09-16） | 结论 |
|---|---|---|
| 剩余 236 块，约 22 万字符中文 | 实际完成 226 块（36 → 262），净 174,712 字符（总净 202,010 − 初期 27,298） | 块数接近；**字符数高估约 26%** |
| 还需 30–40 轮 | 实际 09-16 当天 33 次管线运行（`run_start` 事件；09-15 为 24 次） | 与预测接近，但每轮批量投喂的块数远高于预估 |
| 失败率维持 21%（还会被拦 45–50 次） | 实际失败尝试率 42/479 = **8.8%** | **明显好于预测**，主要是术语表与提示模板在前期已经收敛 |

**校准结论**：我在块数上估得准，在**字符产出上高估约四分之一、在失败率上悲观一倍以上**。
三条偏差都指向同一个原因——我用**项目早期的状态**外推了整条曲线，
而这条曲线的斜率在术语表稳定后是变好的。

---

## 附：本复盘引用的可核对位置

| 内容 | 位置 |
|---|---|
| 完整失败与重试记录 | `logs/ai-journal.md`、`logs/journal.jsonl` |
| 表格行数差异与例外登记 | `reports/table-integrity.md`、`pipeline/config/table-exceptions.json` |
| 术语被判违规 vs 被豁免 | `reports/terminology-consistency.md` 第一节及其「一·附」 |
| 判定口径变更、丢行、术语裁决三件事 | `logs/journal.jsonl`：`qc_criterion_fix`、`table_row_loss_found`、`term_decision` |
| 丢行与机制化的完整叙述 | `notes/拿来说明/04-三处丢行与手工发现的机制化.md` |
| `figure` bug 的修复 | commit `6f35d22` |
| 章节级样板剔除 | commit `c75ff9b` |
| 术语表冲突的发现与修复 | commit `50469eb` → `5eb050a` |
| 术语表定义修正（第 6 次拦截） | commit `e7b20a1` |
| 状态键不一致的修复 | commit `cc5f257` |
| 覆盖度进度 | `reports/coverage.md` |
| 资料清点与缺口 | `source/INVENTORY.md` |
| 清洗前后对比 | `reports/clean-comparison.md` |
